{
  "name": "beaker-new-dev",
  "version": "0.06",
  "description": "Webizen project",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && cpx 'src/**/*' dist && echo 'Building project...'",
    "test": "jest"
  },
  "dependencies": {
    "electron": "^32.1.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "jest": "^29.7.0",
    "rimraf": "^3.0.0"
  },
  "jest": {
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    },
    "moduleNameMapper": {
      "\\.(css|less|scss|sass)$": "identity-obj-proxy"
    },
    "testEnvironment": "node"
  }
}
